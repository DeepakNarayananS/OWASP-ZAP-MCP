# OWASP ZAP MCP configuration
# Copy this file to .env and fill in your real ZAP API key.
# Find it in ZAP: Tools > Options > API.
# .env is gitignored, so your key stays out of version control.
ZAP_API_KEY=your-zap-api-key-here
